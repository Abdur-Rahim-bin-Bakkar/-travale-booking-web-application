import { Button } from '@heroui/react';
import React from 'react';

const Navbar = () => {
    return (
        <div>
            <Button>Click Me</Button>
        </div>
    );
};

export default Navbar;